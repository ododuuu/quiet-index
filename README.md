# LocalDocSearch

LocalDocSearch 是一套純本機 Windows CLI，可為 Markdown、純文字、現代 Office 與文字型 PDF 建立索引並進行搜尋。

本專案目前位於 M0 里程碑：專案基礎與規格建立。

## 開發方式

```powershell
npm.cmd install
npm.cmd test
npm.cmd start -- --help
```

產品行為請閱讀 `docs/SPEC.md`，下一項工作請閱讀 `docs/STATUS.md`。
