# Seekah 0.37.0 後續交接（尚未開工）

前置：先完成 0.36.1／SPEC §45，CURRENT 未切換前不要實作本文件。

權威規格：SPEC §46.1～46.10；決策 D055／D056。原始根因與分階段細節保留在 [歷史交接](../HANDOFF.md)。

順序：
1. 基線：正常使用者驗證現有 autoupdate 的 file event→applyPathChange，不呼叫 index；記錄事件延遲與 root scan 次數。用正確 shell profile 取得完整校正各階段成本。
2. 持久 queue：獨立版本化 SQLite、先落盤再接受、index commit 後按世代 ack、crash 安全重播、超限先保存 dirty scope；不能宣稱跨庫原子交易。
3. 有界 watcher scopes：依 §46 上限／降級、rename／新目錄競態；失敗範圍最小化但不得失去覆蓋。
4. 分批背景校正：保存可恢復工作狀態，讓出 writer，安全處理 scan 與 event 交錯；停機期間沒有可靠事件，啟動校正補回，未完成顯示未確認。
5. status 與普通使用者 opt-in 登入啟動；公司政策拒絕仍能手動 start，不提權、不裝 Service、不研究 USN。
6. all-terms 先 benchmark 再修長詞＋短詞 pruning；中文／英文／跨 payload／全短詞與基線逐筆等價，不能 false negative。

細節與參數不得取代 SPEC；每步補故障測試。不要把只加速完整掃描當作日常變更發現已完成，不改 parser selection。完成更新同資料夾、STATUS／DECISIONS／NEXT-TODO、README 與版本驗證報告。
