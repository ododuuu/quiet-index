# Seekah 0.36.2 GUI 實作交接

日期：2026-09-24。狀態：設計已核准、SPEC 已建立；程式尚未實作，package 仍為 0.36.1。
指定實作者：Luna Max。這是使用者指定的接手模型，不表示本工作階段已切換模型或已啟動該模型。

## 權威與入口

先依 AGENTS.md 讀取 SPEC、STATUS、DECISIONS，再讀 CURRENT 與本文件。

- 行為：[SPEC §47](../SPEC.md#47-0362核准的本機-gui-工作台改版)，安全沿用 §43。
- 視覺：[SEEKAH-WORKBENCH.md](../design/SEEKAH-WORKBENCH.md)、[HTML](../design/seekah-workbench.html)。
- 決策：D059。0.37.0 §46 暫緩，不能和本批混做。
- 從 GitHub 最新 main 建立實作分支；保留未提交的使用者工作，不 reset 或覆蓋。

## 已完成

- 桌面／行動版互動設計稿與預覽圖已在 main。
- §47 定義版面、資料來源、狀態機、API 補齊、安全、無障礙與七組驗收門檻。
- 將原型缺口明列：容量必須來自 server bytes、索引狀態必須唯讀查庫、完整片段不等於全文、Key／model 設定與 AI 回答不能漏做。

## 未完成／執行順序

1. 檢查 `src/workbench-app.ts`、`src/workbench.ts`、`src/workbench-context.ts`、`src/workbench-provider.ts`、`src/mcp-tools.ts` 與 `test/m35.test.ts` 的現行契約。重用既有服務，不改搜尋或 parser 核心。
2. 以正式 API 重做三區版面、搜尋提交快照／分頁、跨查詢選取、命中片段預覽、臨時文件及抽屜。使用真實資料，不嵌入 docs HTML 的示範陣列或假成功回覆。
3. 補唯讀 `/api/index-status`，映射已有 store／indexStatus，展示真實根目錄、計數與歷史同步；無資料顯示未知，不新增 watcher 日誌或自動掃描。
4. 完成連線設定（Provider、model、Key）、精確預覽／bytes、複製、同意失效、外部明確送出與回答。處理較晚回覆與重複點擊；不能只重做外觀卻漏掉正式 API 閉環。
5. 完成深淺色、五種尺寸、鍵盤焦點、對話框／抽屜 inert 與來源字串安全。正式 CSP 不可放寬為 unsafe-inline。
6. 依 §47.8 啟動真正 `ui --no-open`、建立隔離合成索引、操作瀏覽器，拍攝正式畫面。Provider 以注入捕捉器驗證，不花真實 API 額度或上傳公司資料。必要行為回歸後跑完整 npm test、Node 22.17.0 與 package。
7. 全部驗收後才升 package／lockfile 至 0.36.2，建立 0.36.2-VALIDATION.md，更新 README、STATUS、DECISIONS、CURRENT、本交接；推送 main 並提供連結。

## 驗證狀態

目前僅核對規格與正式路由；未修改或執行 0.36.2 程式。原型瀏覽器驗證與 0.36.1 測試均不能代替本版驗收。公司 Windows、真實 Provider 仍待對應環境驗證。

## 禁止擴張

不改 TUI、不搬 LocalDocSearch 資料目錄、不改 schema／parse version、不要求 rebuild、不刪 WAL／journal；不做 0.37.0 queue／scope／startup、OCR、串流回答、自動重試、Key 持久化或新的根／格式篩選。
