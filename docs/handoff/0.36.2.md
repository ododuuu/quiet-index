# Seekah 0.36.2 GUI 實作交接

日期：2026-09-24。狀態：正式 GUI 已實作並完成本機驗收；package／lockfile 已升為 0.36.2。
指定實作者：Luna Max。這是使用者指定的接手模型，不表示本工作階段切換了模型。

## 權威與範圍

先依 AGENTS.md 讀取 SPEC、STATUS、DECISIONS，再讀 CURRENT 與本文件。

- 行為：[SPEC §47](../SPEC.md#47-0362核准的本機-gui-工作台改版)，安全沿用 §43。
- 視覺：[SEEKAH-WORKBENCH.md](../design/SEEKAH-WORKBENCH.md)、[HTML](../design/seekah-workbench.html)；HTML 只作設計原型。
- 決策：D059／本批實作紀錄。0.37.0 §46 暫緩，不能和本批混做。
- 保留 LocalDocSearch 資料目錄、環境變數、ignore、IPC／MCP 識別、TUI、CLI、parser selection；不 reset 或覆蓋使用者工作。

## 已完成

- `src/workbench-app.ts` 已改為正式三區自足 GUI：搜尋、分頁、提交快照、跨查詢去重選取、命中片段預覽、臨時文件、狀態頁、抽屜、設定對話框、深淺色與行動底部導覽。
- `src/workbench.ts` 重用既有 search／context／parser／Provider，新增受 token／Host／Origin 保護的唯讀 `GET /api/index-status`；狀態只讀資料庫，不掃來源、不啟動 watcher、不升級索引。
- 精確預覽只顯示 server 回傳 context 與 bytes；複製不需 Key。Provider／model／Key、明確同意、HMAC preview、回答、auto 路由與一次 quota fallback 均閉環，送出確認碼單次消費以避免重複計費。
- `src/mcp-tools.ts` 的既有 `indexStatus` 增加逐根文件數、同步摘要與錯誤欄位，未改 MCP 工具的唯讀邊界。
- `test/m35.test.ts` 覆蓋 GUI 自足性、狀態 API、Host／token、精確 bytes、stale preview、單次 ask、auto fallback、Key 不回顯與臨時檔清理。

## 驗證

- 詳細命令與證據見 [`docs/0.36.2-VALIDATION.md`](../0.36.2-VALIDATION.md)。已使用隔離合成索引啟動真正 `ui --no-open`，以 Chromium 操作 1280×800 搜尋／分頁／選取／精確預覽／複製／狀態頁，並以 390×844 驗證行動版無水平溢位與設定入口。
- Provider 使用注入 fake fetch；未呼叫真實付費 API、未使用公司文件。
- 公司 Windows、Node.js 22.17.0 實機、真實 Provider 與完整截圖檔案仍未在本環境完成，不能宣稱已通過。

## 禁止擴張

不改 TUI、不搬資料目錄、不改 schema／parse version、不要求 rebuild、不刪 WAL／journal；不做 0.37.0 queue／scope／startup／all-terms 效能、OCR、串流回答、一般自動重試、Key 持久化或新的根／格式篩選。
