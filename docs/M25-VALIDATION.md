# M25 0.28.0 驗證

M25 依 SPEC §36 實作結果內搜尋。互動 `search` 以 `/ 關鍵字` 對目前完整命中集合追加文件級 AND，核對檔名、已索引標題與全部已索引正文；`back`／`reset` 可逆，單頁與零結果仍可操作。縮小後保留最初相對排序，代表片段對應最新一層條件。

本機自動測試涵蓋：第 21 筆之後的文件、原片段外正文、檔名加正文跨層、多層跨區塊 AND、片語與 `--all-terms`、根目錄／格式隔離、metadata-only、回退／重設／零結果、分頁不重複、外部提交中止、唯讀開庫，以及非互動 CLI 相容。

2026-09-22 在 Linux 開發沙盒執行 `npm test`：167 項中 164 通過、1 失敗、2 略過。失敗項為既有 M7 真實開啟在非 macOS／Windows 平台回報 `ACTION_PLATFORM_UNSUPPORTED`，與本次搜尋工作階段無關。略過項為 M5 無法讀取目錄（此環境）及 Windows cmd 專屬 launcher。不得以本沙盒結果取代 macOS 完整回歸或公司 Windows 驗收。

公司 Windows 請實際執行「初搜 → 多次 `/ 關鍵字` → `n`／`p` → `back` → `reset` → `q`」。0.27.0 人工驗收成功不代表 0.28.0 已通過。交付包 `LocalDocSearch-M25-0.28.0.zip`，SHA-256 `ee52ecfa2edc39e2f387a38db11700b812285a8524b59043686e5de06542e5d5`。
