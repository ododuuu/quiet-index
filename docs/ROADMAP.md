# LocalDocSearch 使用導向路線圖

更新：2026-09-22。以自己每天用得順為成功標準，作品集／在職碩為加值，星數不是目標。

最新優先事項：M25（0.28.0）結果內搜尋已實作。互動 `search` 可用 `/ 關鍵字` 在完整命中集合逐層縮小，back 撤回、reset 回到最初集合，並顯示條件鏈與目前／最初總數。公司 Windows 實機「初搜 → 追加 → 翻頁 → back → reset」仍待驗收。詳見 SPEC §36／D044。

## 目標與界線

已排入 M25 之後：M26 根目錄範圍合併（預定 0.29.0）。支援從 `D:\備份` 擴大至 `D:\`，原子轉移既有文件歸屬並保留排除規則與文件代碼；詳見 SPEC §37。規劃完成，尚未實作。

只索引自己有權限的目錄，靠內文找到文件並打開使用。目前以使用者的 macOS 電腦持續迭代，維持 Node.js 22.17.0 以上、可攜 CLI、純本機，並保留 Windows 相容方向。與檔名工具互補，不做全碟檔名引擎或文件管理系統。

預設不做 OCR、GUI、自動向量 RAG、整庫灌入模型、MCP 或 AI；後續是否開發以日常痛點為依據。掃描件若成為主痛，OCR 另規劃為預設關閉的選配。公司內容不外傳，不繞過權限。

## 依序交付

| 階段 | 具體成果 | 驗證依據／目前缺口 |
|---|---|---|
| M6 格式地基 | DOC／XLS／網頁／MSG 與 VSD v11 直接文字 | 0.9.0 本機 63 項測試通過；Windows 日常文件仍待驗收 |
| macOS 日常主線 | 解壓→安裝依賴→index→search→open→context 可重現 | M14 起以目前 Mac 的真實互動流程作逐版證據；Windows 驗收延後 |
| M7 找檔閉環 | 對明確搜尋結果開啟文件、顯示所在資料夾 | 0.10.0 已實作固定文件代碼、open／reveal／dry-run；本機 71 項測試通過，Windows 開啟及顯示仍待驗收 |
| M8 多根目錄 | 同時涵蓋多個有權限位置，不因換根目錄清掉其他索引 | 0.11.0 已實作多根隔離、移除、離線保留、跨根搜尋及舊索引遷移；78 項本機測試通過，Windows 待集中驗收 |
| CLI 減少摩擦 | 視需要提供 .cmd 或捷徑 | 預設不新增大 GUI；以日常操作次數判斷 |
| 選配上下文（M9→M14） | JSON／Markdown 多段命中；跨查詢累積規格、文件與 BU 聊天 | M14 0.17.0 在 macOS 真實互動終端驗證；預設不接模型 |
| M15 本機交付 | 完整預覽與 yes 後，選擇新檔或本機剪貼簿 | 0.18.0 自動測試完成；不連接外部服務，Windows Unicode 剪貼簿待實機驗證 |
| M16 查詢容錯 | 顯式 `--all-terms`，讓記得的多個詞可分散於同一文件 | 0.19.0 本機完成；預設片語語意不變，未加入模糊／同義詞 |
| M17 完整檔案清冊 | 登錄所有未排除的一般檔案；未支援格式可依檔名與類型找到並開啟 | 0.20.0 本機完成；metadata-only 不讀正文，下一步續索引瘦身 |
| 監看自動增量（M11） | 前台 `watch`＋防抖增量 sync | 0.14.1 修正啟停；M12 0.15.0 定期校正／離線恢復；macOS 原生監看已測，Windows 待集中驗收 |

只有 STATUS 指定的階段可開始實作。新增階段前先補 SPEC 與設計決策。Windows 未驗證資訊保留，但不再阻擋 macOS 主線迭代。

## 格式擴充停損

現有盤點用於排序，不代表所有文件都有業務價值，也不是 2.83 GB 工作負載的分布。0.9.0 先交付 VSD v11 直接文字後，等待真實失敗代碼及使用頻率，再決定是否投資較舊 VSD、master／動態欄位或下一種格式。格式未覆蓋不阻止規劃找檔閉環；不因參考專案列了功能就追齊。

不新增讀取全機內容的盤點工具。需要再盤點時沿用 Windows 命令，只統計副檔名數量；無權限位置跳過。2.83 GB 首次索引約一分鐘是使用者觀察，不能外推 200 GB 或新版效能。

## 未來 AI 上下文模式

A：使用者自行搜尋，再選取結果送入上下文。B：使用者觸發並輸入關鍵字／問題，可選查詢改寫，搜尋後仍由使用者選取並確認。兩模式共用路徑＋命中段落的選擇機制；文件與 BU 聊天可列來源候選，聊天匯入格式及權限尚未定義，不宣稱已實作。

預設對話不自動載入整庫；人選上下文是產品功能。embedding 僅在同義詞與關鍵字落差成為主要問題後評估。公司不能外傳時，保持本機或使用已允許的通道，不能因使用者勾選就假設外傳已核准。

## 作品集加值

保留問題、職場限制、取捨、可跑示範、測試／實測證據及反思。AI 功能不是作品集的先決條件，產品日常可用與公司環境可跑優先。

M12（0.15.0）已補定期校正與離線恢復；M13（0.16.0）已補跨程序寫入協調。本機 116 項測試通過、1 項 Windows cmd 略過；Windows 日常使用的成功標準仍待集中驗收，不以持續擴充功能替代。
